/**
 * @enum {user.role} role
 *
 * @field admin: administrator
 * @field groupOwner: owner of a group
 * @field user: basic user
 */
export const enum role {
  admin = 'admin',
  groupOwner = 'groupOwner',
  user = 'user'
}
